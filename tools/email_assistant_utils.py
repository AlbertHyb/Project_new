def show_graph(graph=None, xray=False):
    """Función para mostrar el grafo (stub simple)."""
    print("Grafo simulado: Nodos = triage_router, response_agent, END")
    if graph:
        print(f"Detalles: {graph}")
    if xray:
        print("Modo XRAY activado (solo simulación)")